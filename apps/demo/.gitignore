# Localnet artifacts produced by scripts/dev-localnet.sh and
# scripts/dev-surfpool.sh. Contains keypairs and a market PDA — both
# ephemeral, both per-machine.
.localnet/
.localnet.pid
.surfpool.pid

# Devnet deploy payer keypair (per-machine secret).
.deploy-payer.json

# Vite env files (also covered by repo root, repeated here for clarity).
.env
.env.local
.env.*.local

# Playwright e2e artifacts
/test-results
/playwright-report
/playwright/.cache
/e2e/.e2e-fixture.json

# Stray backups of the generated env file.
.env.local.bak*
