# app.stooks.xyz → stookstreet.xyz. The app itself is deployed from site/.
name = "stook-app"
main = "redirect.js"
compatibility_date = "2026-08-01"
routes = [{ pattern = "app.stooks.xyz", custom_domain = true }]
