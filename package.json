{
  "name": "stook",
  "version": "0.0.0",
  "private": true,
  "description": "Stook \u2014 price-ladder prediction markets on Solana",
  "license": "Apache-2.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "packageManager": "pnpm@9.0.0",
  "pnpm": {
    "overrides": {
      "@solana/web3.js": "^1.98.0"
    }
  }
}
