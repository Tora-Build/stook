{
  "name": "soo",
  "version": "0.0.0",
  "private": true,
  "description": "Sooth Protocol on Solana — Anchor programs and TypeScript SDK adapter",
  "license": "Apache-2.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "packageManager": "pnpm@9.0.0"
}
